# Shot assets

Everything in this folder is a slot the film reads at runtime. Nothing here is
generated by the build — drop the approved files in and they appear. While a
slot is empty the film degrades honestly: the video layer stays black and the
device screen stays dark. It never substitutes a fabricated interface.

Follow the keyframe-first workflow. Generate the still, judge it against the
shot spec, and only extend an approved still to video. A weak keyframe is
regenerated, never rescued with motion.

## Generated plates

| File | Shot | Notes |
|---|---|---|
| `01.jpg` | 01 — The world before AXON | Approved keyframe. Doubles as the video poster and as the mobile still. |
| `01.mp4` | 01 | Slow continuous forward push. H.264, 1920×1080, no audio. |
| `02.jpg` | 02 — Fragmented memory | Approved keyframe. Five sources belonging to one person: conversation, note, document, image, idea. |
| `02.mp4` | 02 | Slow lateral glide, surfaces parallaxing at different rates. |

Keep both clips under ~4 MB. They are `preload="none"` and only fetched when
the film reaches them, but they still sit in front of the story.

Mobile drops the `src` and shows the poster, so the two stills must stand on
their own as compositions.

## Real product UI

| File | Shots | Notes |
|---|---|---|
| `memory-screen.png` | 07, 10 | A real screenshot of the AXON memory detail view showing **API Architecture Decision**. |

Portrait, roughly 1170×2470, dark theme, no browser chrome, no status bar
overlay. Shots 07 and 10 must show the *same* memory — that is what keeps the
film internally consistent and the product positioned as a general memory
layer.

This one cannot be faked. The brief forbids generated app UI, and the device
screen is deliberately left dark until this file exists.
